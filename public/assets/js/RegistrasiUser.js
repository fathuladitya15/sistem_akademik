(function name() {
	"use strict";

	$(document).ready(function () {
		function triggerClick(elem) {
			$(elem).click();
		}
		var $stepper = document.querySelector("#kt_create_account_stepper"),
			$tab_current,
			$tab_prev,
			$tab_next,
			$btn_prev = $stepper.find();
	})
})